# Stuart v1 (Ashby meetings module) — dependency bands
#
# Intent:
# - Make a new-machine install possible without guessing.
# - Keep versions inside known-safe ranges (upper bounds prevent surprise breakage).
# - Torch is intentionally NOT hard-installed here because CPU/GPU wheels vary by machine.
#   Install torch first (or last) with the correct wheel for your platform.
#
# Recommended workflow:
#   cd Ashby_Engine
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements-stuart-v1.txt
#   python3 scripts/stuart_preflight.py --strict
#
# For ship-time exact freeze:
#   pip freeze > requirements-stuart-v1.lock.txt

# Web door
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0  # optional: browser form uploads

# Optional: remote formalizers (minutes/journal) when ASHBY_MEETINGS_LLM_ENABLED=1
openai>=1.30,<2.0

# Real engines (local)
# NOTE: faster-whisper uses CTranslate2 (ctranslate2).
faster-whisper>=0.10,<1.1
ctranslate2>=4.0,<5.0

# Diarization (requires torch + gated HuggingFace access for some models)
pyannote.audio>=3.1,<4.0
huggingface_hub>=0.20,<1.0  # pyannote compatibility: hf_hub_download(use_auth_token=...) path breaks on hub 1.x

# PDF rendering (requires OS Cairo/Pango stack)
weasyprint>=60,<63
# Compatibility pin: WeasyPrint 60-62 is incompatible with pydyf>=0.11
# (runtime error: "'super' object has no attribute 'transform'")
pydyf>=0.8,<0.11

# Verification tooling (optional but recommended)
pytest>=8.0,<9.0
